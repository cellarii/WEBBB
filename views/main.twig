
{% extends "__layout.twig" %}

{% block content %}
<div class="row d-flex">
    {% for object in vasteras_area %}
    <div class="col mb-4 d-flex align-items-stretch">
        <div class="border p-2 d-flex flex-column justify-content-between w-100">
            <div class="img-thumbnail mb-2" style="width: 100%; height: 200px; overflow:hidden">
                <img src="{{ object.image }}" style="height: 100%; width: 100%; object-fit: cover;" alt="">
            </div>
            <div class="d-flex flex-column">
                <a href="/vasteras-area/{{object.id}}" type="button" class="btn btn-primary mb-1">{{ object.title }}</a>
                <div class="d-flex justify-content-center">
                    <a class="me-3" href="/vasteras-area/{{object.id}}/image">Картинка</a>
                    <a href="/vasteras-area/{{object.id}}/info">Описание</a>
                </div>
            </div>
        </div>
    </div>
    {% endfor %}
</div>
{% endblock %}

{# {% block content %}
{% from 'macros.twig' import btn %}
<div style="display: flex;">
    {% for object in vasteras_area %}
        <div class="block">
            <div class="block-img">
                <img src="{{ object.image }}" alt="" width="200px" height="150px">
            </div>
            <a href=""><button type="button" class="btn btn-primary" style="width: 200px">{{ object.title }}</button></a>
            <div>
                <a href="">Картинка</a>
                <a href="">Описание</a>
            </div>
        </div>
    {% endfor %}
</div>
{# <pre>
    {{ dump(vasteras_area)}}
</pre>
<div class="list-group">
{% for item in menu_items %}
{% include "items.twig"%}
{% endfor %}
</div> #}